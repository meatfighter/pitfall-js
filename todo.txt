uint8_t A = random2;
A <<= 1;         // ASL
A ^= random2;    // EOR
A <<= 1;         // ASL
random2 = (random2 << 1) | ((A & 0x100) ? 1 : 0);  // approximate ROL

bit 4 is used to shake cobra, fire, gold, and silver